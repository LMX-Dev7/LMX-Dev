/* ── LMX — interacciones base ─────────────────────────────
   El nuevo nav es minimal (solo marca + botón de ancla), sin
   drawer móvil. El scroll suave lo maneja `scroll-behavior`
   en CSS. Este archivo queda como punto único para futuras
   interacciones globales.
   ────────────────────────────────────────────────────────── */
(function () {
  'use strict';
  // Reservado para interacciones globales futuras.
})();
